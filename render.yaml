services:
  - type: worker
    name: trading-bot
    env: docker
    plan: starter
    repo: "https://github.com/YourUsername/YourRepo.git"
    branch: main
    dockerfilePath: Dockerfile
    autoDeploy: true
